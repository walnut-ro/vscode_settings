{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "make re",
            "options": {
                "cwd": "${workspaceFolder}/Push_swap"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
